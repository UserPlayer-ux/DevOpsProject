name: DevOps Project CI/CD
run-name: ${{ github.actor }} is deploying the F1 Driver Web App 🚀

on: [push]

jobs:
  Deploy-F1-Driver-App:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Build application
        run: npm run build

      - name: Deploy to AWS
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: 'us-east-1'
        run: |
          aws s3 sync ./build s3://your-f1-driver-app-bucket --delete

      - name: Confirm Deployment
        run: echo "🚀 The F1 Driver Web App has been successfully deployed!"
